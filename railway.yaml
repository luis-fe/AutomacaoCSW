version: 2
environments:
  - name: default
    # Instalação do JDK
    scripts:
      - name: Instalar JDK
        script: |
          sudo apt-get update
          sudo apt-get install default-jdk

    # Configuração do JAVA_HOME
    env:
      JAVA_HOME: /usr/lib/jvm/default-java
